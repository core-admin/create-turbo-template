{
  "name": "create-turbo-vben",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "turbo run build --filter=./apps/* --parallel",
    "dev": "turbo run dev --filter=./apps/* --parallel"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/node": "^18.11.18",
    "esno": "^0.16.3",
    "rimraf": "^3.0.2",
    "turbo": "^1.6.3",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "fs-extra": "^11.1.0"
  }
}